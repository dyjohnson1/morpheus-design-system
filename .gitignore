node_modules/
dist/
build/
storybook-static/
*.log
.DS_Store
.env*

# Local MCP config (may contain API tokens)
.kiro/settings/mcp.json

# L'enchanteur client project — lives in its own repo (github.com/dyjohnson1/lenchanteur)
/lenchanteur/
